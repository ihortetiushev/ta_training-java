package final_task;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.edge.EdgeDriver;

public class FinalEdgeTaskTest extends TestBase {

    FinalEdgeTaskTest() {
        super(new EdgeDriver());
    }
}
